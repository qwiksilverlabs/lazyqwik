name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v2
    with:
      publish: true
      build: pnpm exec vp run build
    permissions:
      contents: write
      id-token: write
